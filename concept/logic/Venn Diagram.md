
Group/Set Logic 
- **集合（Circle）**：每个圆代表一个主题或事物类别（例如：集合 A、集合 B）。
    
- **交集（Intersection，图中标红区域）**：圆圈重叠的区域，代表**同时满足两边特征**的元素（既是 A 也是 B，记作 $A \cap B$）。
    
- **差集/独有部分（Difference）**：只属于 A 或只属于 B 的区域。
    
- **并集（Union）**：包含所有属于 A 或属于 B 的总区域（记作 $A \cup B$）。